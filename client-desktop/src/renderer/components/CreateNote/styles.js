import styled from 'react-emotion';

const CreateButton = styled('button')`
  background: transparent;
  border: none;
  cursor: pointer;
  :focus {
    outline: none;
  }
  font-size: 1.2rem;
  padding: 0 0.7rem;
`;

export { CreateButton };
